[build-system]
requires = ["setuptools>=64"]
build-backend = "setuptools.build_meta"

[project]
name = "lesson-book"
version = "0.1.1"
description = "Local-first, deterministic mistake ledger for trading: record costs and tags, then match similar situations before you act."
readme = "README.md"
requires-python = ">=3.11"
license = "MIT"
keywords = ["trading", "journal", "mistakes", "lessons", "discipline", "cli", "local-first"]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Environment :: Console",
    "Intended Audience :: Financial and Insurance Industry",
    "Operating System :: OS Independent",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Topic :: Office/Business :: Financial :: Investment",
]

[project.scripts]
lb = "lesson_book.cli:main"
lesson-book = "lesson_book.cli:main"

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
testpaths = ["tests"]

